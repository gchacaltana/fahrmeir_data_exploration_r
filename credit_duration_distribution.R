# Instalación de paquetes
# install.packages("Fahrmeir")
# install.packages("ggplot2")

# Cargamos base de datos: credit
library(Fahrmeir)
data(credit)

# Mostramos primeros 6 registros de la base de datos
head(credit)

# Mostramos estructura de la base de datos: data.frame
str(credit)
